import React from 'react';
import ReactDOM from 'react-dom/client';
import Home from './Submission-form-using-useRef/Home'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <>
  <Home/>
  </>
);

